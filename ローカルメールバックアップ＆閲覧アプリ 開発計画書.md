# **ローカルメールバックアップ＆閲覧アプリ 開発計画書**

## **1\. プロジェクト概要**

### **1.1 目的**

お名前.comをはじめとするIMAPサーバー（および将来的なGmail等のクラウドメールAPI）上に保管されているメールをローカル環境（外付けSSD等）に自動/手動でダウンロードし、安全に長期保管（バックアップ）するとともに、過去のメール資産を高速に検索・安全に閲覧できるデスクトップアプリケーションを開発する。

### **1.2 背景とコンセプト**

* **サーバー容量の節約:** メールサーバー容量の逼迫を防ぐため、ローカルへの完全保管後にユーザー操作でサーバー上のメールを任意で削除できるようにする。  
* **ローカルデータの非破壊保存 (Append-Only):** サーバー上でメールが削除・移動されても、ローカルに一度保存したバイナリデータ（.eml）は自動消去せず永続保持する。  
* **可搬性の確保:** データベース（SQLite）とバイナリ（.eml）を同階層で管理し、外付けドライブの挿し替えやドライブレター（D:\\, E:\\等）の変更に柔軟に対応する。  
* **高い拡張性（マルチプロトコル対応）:** メール取得・削除ロジックをインターフェースとして抽象化し、お名前.com等の標準IMAP（Basic認証）から、将来的なGmail/Outlook（OAuth2 \+ XOAUTH2 / REST API）への対応拡張を容易にする。

## **2\. システム構成と技術スタック**

### **2.1 技術スタック**

* **言語:** Python 3.10+  
* **GUIフレームワーク:** PySide6 (Qt for Python)  
  * レンダリングエンジン: QWebEngineView（HTMLメールの安全な表示）  
* **データベース:** SQLite 3.34+ (標準搭載の FTS5 \+ trigram モジュール使用)  
* **通信・プロトコル:**  
  * 標準ライブラリ imaplib, ssl（お名前.com IMAP over SSL / Port 993）  
  * 将来対応用: google-auth-oauthlib, keyring（OS標準の安全な資格情報ストア）  
* **メール解析:** 標準ライブラリ email

### **2.2 リポジトリ構成案**

```Plaintext
mail_app/
│
├── config.py                 # 設定管理（dataclass / pydantic等）
├── main.py                   # アプリケーション起動エントリポイント
│
├── domain/                   # 【最内層】ビジネスモデル・抽象インターフェース（依存関係ゼロ）
│   ├── models.py             # Email, Account, SearchFilter などの純粋なデータ構造
│   ├── fetcher.py            # BaseMailFetcher (ABC) の定義
│   └── repository.py         # BaseMessageRepository (ABC) の定義
│
├── usecases/                 # 【内層】ユースケース（具体的な業務ロジック）
│   ├── sync_mail.py          # メール同期処理（Fetcherで取得し、EML保存＆DB登録を行うフロー）
│   ├── search_mail.py        # 検索条件の受け取りとリポジトリ呼び出し
│   └── delete_mail.py        # サーバー側削除の制御ロジック
│
├── infrastructure/           # 【外層：技術的実装】データベース、通信、ファイル入出力
│   ├── fetchers/             # 通信アダプター
│   │   ├── onamae_imap.py    # OnamaeImapFetcher (BaseMailFetcherの実装)
│   │   └── gmail_oauth.py    # (将来用) GmailOAuthFetcher
│   ├── database/             # DB処理
│   │   ├── connection.py     # SQLite接続・テーブル作成・FTS5トリガー
│   │   └── repository.py     # SQLiteMessageRepository (BaseMessageRepositoryの実装)
│   ├── storage/              # ファイルシステム処理
│   │   └── eml_storage.py    # EMLファイルの保存・読み込み・ハッシュ計算
│   └── security/             # セキュリティ処理
│       └── keyring_store.py  # keyringライブラリを用いた安全なパスワード/トークン保管
│
├── presentation/             # 【最外層：UI】PySide6固有の処理
│   ├── views/                # QWidget, QMainWindow などの画面レイアウト
│   │   ├── main_window.py    # メイン画面
│   │   ├── message_list.py  # QTableView（メール一覧）
│   │   └── detail_view.py   # QWebEngineView（メール本文表示）
│   ├── viewmodels/           # 画面とユースケースを繋ぐロジック/状態管理
│   └── threads/              # PySide6の QThread (バックグラウンド同期・検索用)
│
└── tests/                    # テストコード（pytest等）
    ├── unit/                 # ドメイン・ユースケースの単体テスト
    └── integration/          # DBやIMAPの結合テスト
```

### **2.3 アーキテクチャ（プロトコル抽象化パターン）**

UI層・DB保管層と通信層を独立させるため、アダプターパターンを採用する。

               \[ UI (PySide6) / Local DB / EML Storage \]  
                                   │  
                     \[ BaseMailFetcher (抽象クラス) \]  
                                   │  
         ┌─────────────────────────┴─────────────────────────┐  
         ▼                                                   ▼  
\[ OnamaeImapFetcher \]                              \[ GmailOAuthFetcher \]  
 (Basic Auth \+ imaplib)                             (将来実装: OAuth2 \+ XOAUTH2)

### **2.4 保存先ディレクトリ・ストレージ構造**

データ保存先（外付けSSD等）を「ストレージルート」とし、相対パスで一括管理する。
保存先ディレクトリはアプリ起動時に選択可能で、DB・EMLファイルはすべてこのルート下に配置する。

\[ユーザー指定ストレージルート\] (例: E:\\MailArchive\\)  
├── metadata.db                  \# メタデータ・インデックス用SQLite DB  
└── eml/                         \# 生バイナリデータ保存フォルダ  
    └── {account\_id}/  
        └── 2026/  
            └── 07/  
                └── {Message-ID\_Hash}.eml

## **3\. データベース設計 (SQLite)**

### **3.1 アカウント管理テーブル (accounts)**

CREATE TABLE IF NOT EXISTS accounts (  
    id TEXT PRIMARY KEY,             \-- アカウントID (例: "user@example.com")  
    provider\_type TEXT NOT NULL,     \-- プロバイダ種別 ('onamae\_imap', 'gmail\_oauth' 等)  
    display\_name TEXT,               \-- 表示名  
    created\_at DATETIME DEFAULT CURRENT\_TIMESTAMP  
);

### **3.2 メッセージメタデータテーブル (messages)**

CREATE TABLE IF NOT EXISTS messages (  
    id INTEGER PRIMARY KEY AUTOINCREMENT,  
    message\_id TEXT UNIQUE,          \-- メールヘッダーの Message-ID (重複判定用)  
    account\_id TEXT NOT NULL,        \-- アカウント識別子 (FK \-\> accounts.id)  
    folder\_name TEXT NOT NULL,        \-- IMAPフォルダ名 (INBOX, Sent など)  
      
    \-- 状態管理フラグ  
    sync\_status TEXT NOT NULL DEFAULT 'synced',   
    \-- 'synced'         : サーバー・ローカル双方に存在  
    \-- 'remote\_deleted' : サーバー上からは削除済み（ローカル保管のみ）  
    \-- 'local\_deleted'  : ローカルアプリ上でも削除済み  
      
    relative\_path TEXT NOT NULL,     \-- EMLファイルへの相対パス (例: eml/account1/2026/07/hash.eml)  
    file\_hash TEXT,                  \-- SHA-256ハッシュ (整合性検証用)  
      
    \-- 主要表示ヘッダー  
    subject TEXT,  
    sender TEXT,  
    recipient TEXT,  
    date\_sent DATETIME,  
    size\_bytes INTEGER,  
      
    last\_seen\_at DATETIME,           \-- 最後にサーバーで確認できた日時  
    created\_at DATETIME DEFAULT CURRENT\_TIMESTAMP,  
    FOREIGN KEY (account\_id) REFERENCES accounts(id)  
);

### **3.3 全文検索用仮想テーブル (messages\_fts)**

CREATE VIRTUAL TABLE IF NOT EXISTS messages\_fts USING fts5(  
    subject,  
    body\_text,                       \-- 本文（HTMLの場合はタグ除去後のプレーンテキスト）  
    attachment\_names,                \-- 添付ファイル名（スペース区切り）  
    content='messages',  
    content\_rowid='id',  
    tokenize='trigram'               \-- Trigramトークナイザー（日本語対応・1〜2文字フォールバック機能付き）  
);

## **4\. 機能要件とモジュール設計**

### **4.1 プロトコル抽象化層 (BaseMailFetcher)**

すべてのメール取得プロバイダ（お名前.com、Gmail等）が実装すべき統一インターフェースを定義する。

from abc import ABC, abstractmethod  
from typing import List, Dict, Any

class BaseMailFetcher(ABC):  
    @abstractmethod  
    def connect(self) \-\> bool:  
        """サーバー接続および認証を行う"""  
        pass

    @abstractmethod  
    def fetch\_message\_list(self, folder\_name: str) \-\> List\[Dict\[str, Any\]\]:  
        """指定フォルダのMessage-IDおよびUID一覧を取得する"""  
        pass

    @abstractmethod  
    def download\_eml\_bytes(self, remote\_id: str) \-\> bytes:  
        """対象メールの生のEMLバイナリデータ(RFC822)を取得する"""  
        pass

    @abstractmethod  
    def delete\_remote\_message(self, remote\_id: str, folder\_name: str) \-\> bool:  
        """サーバー上のメールを削除（またはゴミ箱へ移動）する"""  
        pass

    @abstractmethod  
    def disconnect(self):  
        """セッションの終了"""  
        pass

### **4.2 IMAP同期・バックアップ機能**

1. **増分ダウンロード (Append-Only):**  
   * 選択された MailFetcher（例: OnamaeImapFetcher）を通じてサーバーへ接続。  
   * サーバー上の Message-ID 一覧を取得し、DB未登録のメールのみをFetch。  
   * EMLファイルとして保存し、本文・添付ファイル名を抽出してDBおよびFTSインデックスに登録。  
2. **消去検知 (Sync Status Update):**  
   * 同期実行時、DB上で synced のメールがサーバー上に存在しない場合、ステータスを remote\_deleted に変更（EMLファイルは**絶対削除しない**）。

### **4.3 サーバーメール手動削除機能**

1. ユーザーが画面上で選択したメールに対し「サーバーから削除」を実行。  
2. MailFetcher.delete\_remote\_message() を呼び出し、IMAPの STORE \+FLAGS \\Deleted ＋ EXPUNGE 等を発行して完全抹消。  
3. ローカルDBの該当レコードの sync\_status を remote\_deleted に即座に更新。

### **4.4 高速検索機能**

1. **検索対象:** 件名、差出人、本文、添付ファイル名。  
2. **クエリエスケープ & Trigram検索ロジック:**  
   * ユーザー入力を全角/半角スペースで分解。  
   * 入力キーワードをダブルクォーテーションで囲むエスケープ（" \-\> ""）処理を行い、FTS5構文エラー（Parse Error）を完全に防ぐ。  
   * AND/OR検索の切り替えトグルを提供。  
   * FTS5 \+ Trigramにより、1〜2文字は自動的にLIKE相当（Substring）、3文字以上はインデックス超高速検索を実行。

### **4.5 閲覧・UI機能 (PySide6)**

1. **メイン画面:**  
   * 3ペイン構成（左: アカウント・フォルダ・ステータス切替、中央: メール一覧リスト、右: メール本文プレビュー）。  
2. **視覚的ステータス判定:**  
   * remote\_deleted のメールは一覧上でグレーアウト表示・専用アイコンを付与。  
3. **安全なHTML表示:**  
   * QWebEngineView を使用し、JavaScript実行を無効化 (QWebEngineSettings.JavascriptEnabled \= False)。  
   * 外部画像ロードの初期ブロック（プライバシー保護）。  
4. **添付ファイル操作:**  
   * プレビュー画面からワンクリックで添付ファイルを任意の場所へ保存・抽出。

## **5\. 非機能要件 & セキュリティ**

1. **認証・セキュリティ:**  
   * パスワードや将来のOAuth2リフレッシュトークンは、OSの安全な認証ストレージ（keyring ライブラリ利用でWindows Credential Manager等へ保管）を利用。  
2. **応答性 (UI Thread Separated):**  
   * 大量のEML取得・解析・DB書き込み・検索処理はすべて PySide6 の QThread / QRunnable にて非同期実行し、UIがフリーズしない設計とする。  
3. **データ完全性と例外処理:**  
   * 外付けSSDが未接続の状態で起動した場合、エラーダイアログを表示して読み込み専用モードで起動するか、ドライブの再選択を促す。

## **6\. 開発ロードマップ・フェーズ分割**

| フェーズ | 期間目安 | 主なタスク内容 |
| :---- | :---- | :---- |
| **Phase 1: 抽象化層 & IMAPコア** | 1〜2週間 | BaseMailFetcher 定義、OnamaeImapFetcher 実装、EML解析ロジック、データ保存（EML出力 & SQLite書き込み）の検証 |
| **Phase 2: DB & 検索エンジン** | 1週間 | FTS5 Trigramテーブル構築、安全なクエリエスケープ（FTS5QueryBuilder）およびAND/OR検索の実装 |
| **Phase 3: GUI基礎構築 (PySide6)** | 2週間 | 3ペインレイアウト設計、QThreadによる非同期同期処理、QWebEngineViewでの安全なHTML表示の実装 |
| **Phase 4: 統合 & 例外処理** | 1週間 | 手動サーバー削除機能の実装、ドライブ非接続・移動等の例外ハンドリング、お名前.com実機テスト |
| **Phase 5: （将来拡張）Gmail/OAuth2** | 随時 | GmailOAuthFetcher 実装、OAuth2ブラウザ認証フローの追加（コア・UIロジックは無改修で対応可能） |

## **7\. リスク管理方針**

* **データ損失リスク:** サーバー削除操作の前に確認ダイアログ（対象件数・サイズ表示）を必ず挟む。  
* **ドライブレター変更リスク:** DBおよび設定ファイル内での絶対パス参照を排除し、常にストレージルートからの相対パスで計算する仕組みを徹底する。  
* **将来の仕様変更リスク:** プロトコル通信部を BaseMailFetcher として完全に切り離しているため、メールプロバイダの認証仕様変更（OAuth等）にも通信モジュール単体の改修で対応可能。